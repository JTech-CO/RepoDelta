# 변경 내역

## 1.0.3

- 지속되던 Chrome 서비스 워커 전송 오류의 원인을 수정했습니다. 네이티브 `WorkerGlobalScope.fetch`를 `GitHubClient` 속성에 저장한 뒤 `this.fetcher(...)`로 호출하면서 수신 객체가 바뀌었고, 실제 HTTP 요청 전에 `TypeError: Illegal invocation`이 발생했습니다.
- fetch 의존성을 생성 시점에 `globalThis`에 바인딩하여 저장소 API 요청과 설정의 연결 진단이 실제 서비스 워커 수신 객체를 사용하도록 수정했습니다.
- WorkerGlobalScope처럼 수신 객체를 검사하는 fetch 스텁을 이용한 회귀 테스트를 추가했습니다. 이 테스트는 1.0.0-1.0.2 코드에서 실패하고 1.0.3에서 통과합니다.
- 코드베이스에서 저장하거나 전달하는 브라우저 네이티브 메서드를 전수 재검토했으며 현재 다른 동일 유형의 분리 호출은 확인되지 않았습니다.
- 기존 단위 테스트와 오프라인 UI 테스트가 일반 JavaScript fetch 스텁을 주입했기 때문에 Chrome의 WebIDL 수신 객체 검사를 재현하지 못했던 QA 사각지대를 문서화했습니다.


## 1.0.2

- 실제 `fetch` 실패만 `NETWORK`로 분류하도록 수정했습니다. HTTP 응답 이후 처리 오류가 네트워크 오류로 잘못 표시되지 않습니다.
- GitHub API 요청 자체가 실패하면 비표준 헤더를 제거한 최소 헤더 구성으로 한 번만 호환성 재시도합니다.
- 설정에 GitHub API 연결 진단을 추가했습니다. 호스트 권한, 최소 헤더 요청, API 버전 헤더 요청, 세션 토큰 인증 요청을 각각 구분합니다.
- Chrome이 `api.github.com` 필수 호스트 권한을 보류한 경우 사용자 클릭으로 다시 요청하는 복구 버튼을 추가했습니다.
- 토큰 값을 노출하지 않으면서 `TypeError: Failed to fetch` 같은 브라우저 오류 상세를 짧게 표시합니다.

## 1.0.1

- Chrome에서 GitHub API 요청이 실제 HTTP 응답 전에 실패하며 일반적인 네트워크/방화벽 오류로 표시될 수 있던 전송 경로를 수정했습니다. 수동 리디렉션 처리 대신 Chrome의 기본 리디렉션 처리를 사용합니다.
- 사용자 지정 `connect-src` CSP를 제거하고 Manifest V3 기본 CSP와 기존 GitHub 전용 호스트 권한을 사용합니다.
- 리디렉션 후 최종 응답도 `api.github.com`의 허용된 저장소 API 경로인지 검증합니다.
- ETag 재검증과 브라우저 전송 계층의 호환성을 위해 캐시 모드를 `no-store`에서 `no-cache`로 변경했습니다.

[English](CHANGELOG.md)

## 1.0.0 - 2026-09-16

기본 브랜치의 명시적 확인 기준점, 고정 SHA 비교, 파일 필터, 커밋 페이지 처리, 브랜치·이력·저장소 ID 변경 경고, 다중 탭 revision 검증, 로컬 기준점 관리, 백업 병합 가져오기·내보내기, 세션 전용 토큰, EN/KR, 라이트·다크 테마 연동, 팝업, 안내·개인정보 페이지, 의존성 없는 빌드·ZIP 도구, Node 회귀 테스트, 오프라인 브라우저 QA 하네스를 처음 구현했습니다.

방문만으로 확인 완료 처리하지 않으며 GitHub 데이터를 수정하지 않습니다. 실제 확장 설치, 실계정 API 통합, 관리형 브라우저 지원, Chrome 웹스토어 심사는 별도의 미완료 배포 검증 항목입니다.